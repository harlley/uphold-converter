# Instructions to run the project locally

1. Clone the repository 
2. Install dependencies inside the directory project `yarn install`
3. Duplicate the file .env.example, change the variables with your own data and rename it as .env.local
4. Execute `yarn start`

## Demo

Access https://uphold-converter.vercel.app/
